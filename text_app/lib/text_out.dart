import 'package:flutter/material.dart';

class TextOut extends StatelessWidget {
  final String mainText;

  TextOut({this.mainText});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Text(mainText);
  }
}